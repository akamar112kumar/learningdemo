import { FirstLetterCaptitalPipe } from './first-letter-captital.pipe';

describe('FirstLetterCaptitalPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterCaptitalPipe();
    expect(pipe).toBeTruthy();
  });
});
